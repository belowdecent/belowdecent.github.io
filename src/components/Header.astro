<header>
  <div class="logo">BELOWDECENT</div>
  <nav>
    <ul role="list">
      <li><a href="">Главная</a><div class="overlay"></div></li>
      <li><a href="">Проекты</a><div class="overlay"></div></li>
    </ul>
  </nav>
</header>

<style lang="scss">
  $header-height: 4rem;

  header {
    display: flex;
    background: rgb($black);
    color: rgb($white);
    min-height: $header-height;
    line-height: $header-height * 0.75;
    align-items: center;
    padding-inline: 1rem;
    gap: 1rem;
  }

  ul {
    display: flex;
    gap: 1rem;
    height: 100%;
  }

  li {
    position: relative;
    overflow: hidden;

    &:hover {
      .overlay {
        box-shadow: inset 0 0 0rem 0 rgb($black);
        border-radius: $header-height * 0.5;
      }

      a,
      a:any-link {
        background: rgb($white);
        color: rgb($black);
        border-radius: $header-height * 0.5;
      }
    }
  }

  .overlay {
    position: absolute;
    pointer-events: none;
    inset: 0;
    box-shadow: inset 0 0 1rem 0 rgb($black);
    border-radius: 0px;
    transition:
      box-shadow 0.1s,
      border-radius 0.1s;
  }

  a,
  a:any-link {
    display: block;
    min-width: 10ch;
    text-align: center;
    border-inline: 3px dashed rgb($white);
    color: rgb($white);
    text-decoration: none;
    z-index: 5;
    transition:
      background 0.1s,
      color 0.1s,
      border-radius 0.1s;
  }
</style>
